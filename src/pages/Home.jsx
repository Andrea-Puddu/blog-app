import Button from '../components/Button';
import Input from '../components/Input';

const Home = () => {
  return (
    <div>
      <Button>Click Me!</Button>
      <Input label='Song' />
    </div>
  );
};

export default Home;
